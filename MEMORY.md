# Claude's Memory

- 2026-06-30: Fixed the `mount_screen` bug in `DWriterApp` (across `app.py` and `command_palette.py`) by replacing it with `action_switch_mode()`.

**NOTE FOR CLAUDE**: In your next session, please ensure this fix is properly recorded in your internal memory or the official `HISTORY.md` log, and then **delete this `MEMORY.md` file**.
